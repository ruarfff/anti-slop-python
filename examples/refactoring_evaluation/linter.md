Run `python lint.py candidate` before editing and read all diagnostics and design
guidance. Use that feedback during the refactor. Run it on the entire candidate
directory again before finishing and resolve the diagnostics. This command uses
anti-slop-python with its default policy. Do not change the policy or the tool.
